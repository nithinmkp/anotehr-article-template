$if(title)$
\title{$title$$if(thanks)$\thanks{$thanks$}$endif$}
$endif$

$if(author)$
$for(by-author)$
\author[$for(by-author.affiliations)$$it.number$$sep$, $endfor$]{$by-author.name.literal$%
$if(by-author.attributes.corresponding)$\thanks{Corresponding author.\\ Email: \texttt{$by-author.email$}}$endif$%  % Add corresponding author footnote if applicable
  $if(it.acknowledgements)$\thanks{$it.acknowledgements$}$endif$ }
$endfor$
$endif$

$if(by-affiliation)$
$for(by-affiliation)$
\affil[$it.number$]{
  $if(it.department)$
    $it.department$, $it.name$
  $else$
    $it.name$
  $endif$
}
$endfor$
$endif$

$if(date)$
\date{$date$}
$else$
\date{} % Leaves the date blank if no date is provided
$endif$
$if(title)$
\title{$title$$if(thanks)$\thanks{$thanks$}$endif$}
$endif$

$if(author)$
$for(by-author)$
\author[$for(by-author.affiliations)$$it.number$$sep$, $endfor$]{$by-author.name.literal$%
$if(by-author.attributes.corresponding)$\thanks{Corresponding author.\\ Email: \texttt{$by-author.email$}}$endif$%  % Add corresponding author footnote if applicable
  $if(it.acknowledgements)$\thanks{$it.acknowledgements$}$endif$ }
$endfor$
$endif$

$if(by-affiliation)$
$for(by-affiliation)$
\affil[$it.number$]{
  $if(it.department)$
    $it.department$, $it.name$
  $else$
    $it.name$
  $endif$
}
$endfor$
$endif$

$if(date)$
\date{$date$}
$else$
\date{} % Leaves the date blank if no date is provided
$endif$
$if(title)$
\title{$title$$if(thanks)$\thanks{$thanks$}$endif$}
$endif$

$if(author)$
$for(by-author)$
\author[$for(by-author.affiliations)$$it.number$$sep$, $endfor$]{$by-author.name.literal$%
$if(by-author.attributes.corresponding)$\thanks{Corresponding author.\\ Email: \texttt{$by-author.email$}}$endif$%  % Add corresponding author footnote if applicable
  $if(it.acknowledgements)$\thanks{$it.acknowledgements$}$endif$ }
$endfor$
$endif$

$if(by-affiliation)$
$for(by-affiliation)$
\affil[$it.number$]{
  $if(it.department)$
    $it.department$, $it.name$
  $else$
    $it.name$
  $endif$
}
$endfor$
$endif$

$if(date)$
\date{$date$}
$else$
\date{} % Leaves the date blank if no date is provided
$endif$
$if(title)$
\title{$title$$if(thanks)$\thanks{$thanks$}$endif$}
$endif$

$if(author)$
$for(by-author)$
\author[$for(by-author.affiliations)$$it.number$$sep$, $endfor$]{$by-author.name.literal$%
$if(by-author.attributes.corresponding)$\thanks{Corresponding author.\\ Email: \texttt{$by-author.email$}}$endif$%  % Add corresponding author footnote if applicable
  $if(it.acknowledgements)$\thanks{$it.acknowledgements$}$endif$ }
$endfor$
$endif$

$if(by-affiliation)$
$for(by-affiliation)$
\affil[$it.number$]{
  $if(it.department)$
    $it.department$, $it.name$
  $else$
    $it.name$
  $endif$
}
$endfor$
$endif$

$if(date)$
\date{$date$}
$else$
\date{} % Leaves the date blank if no date is provided
$endif$
$if(title)$
\title{$title$$if(thanks)$\thanks{$thanks$}$endif$}
$endif$

$if(author)$
$for(by-author)$
\author[$for(by-author.affiliations)$$it.number$$sep$, $endfor$]{$by-author.name.literal$%
$if(by-author.attributes.corresponding)$\thanks{Corresponding author.\\ Email: \texttt{$by-author.email$}}$endif$%  % Add corresponding author footnote if applicable
  $if(it.acknowledgements)$\thanks{$it.acknowledgements$}$endif$ }
$endfor$
$endif$

$if(by-affiliation)$
$for(by-affiliation)$
\affil[$it.number$]{
  $if(it.department)$
    $it.department$, $it.name$
  $else$
    $it.name$
  $endif$
}
$endfor$
$endif$

$if(date)$
\date{$date$}
$else$
\date{} % Leaves the date blank if no date is provided
$endif$